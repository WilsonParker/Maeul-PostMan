$kind: http-request
name: Itinerary current (현재 진행 중, 진행 예정 일정 조회)
url: "{{server-url}}/api/itinerary/current"
method: GET
body:
  type: json
  content: ""
auth:
  type: bearer
  credentials:
    token: "{{bearer-token}}"
order: 4446579204589835
