$kind: http-request
url: "{{server-url}}/api/itinerary/generate"
method: POST
body:
  type: json
  content: |-
    {
      "tour_id": 0,
      "startDate": "",
      "endDate": ""
    }
auth:
  type: bearer
  credentials:
    token: "{{bearer-token}}"
examples: .resources/Itinerary Generate (일정 생성).resources/examples
order: 3000040394779378
