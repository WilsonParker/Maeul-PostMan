$kind: http-request
url: "{{server-url}}/api/festivals/:contentId"
method: GET
pathVariables:
  - key: contentId
    value: ""
    description: ""
auth:
  type: bearer
  credentials:
    token: "{{bearer-token}}"
examples: .resources/Festival Show (축제 조회).resources/examples
order: 4080384437513754
