$kind: http-request
url: "{{server-url}}/api/surveys"
method: POST
body:
  type: json
  content: |-
    {
      "surveyId": 0,
      "answers": [
       
      ]
    }
auth:
  type: bearer
  credentials:
    token: "{{bearer-token}}"
examples: .resources/Survey Store (설문 답변 저장).resources/examples
order: 2468565629755931
